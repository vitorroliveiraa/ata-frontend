import Form from "@/components/form/index.tsx";

const Registration = () => {
  return (
    <main className="max-w-3xl w-full self-center">
      <Form />
    </main>
  );
};

export default Registration;
